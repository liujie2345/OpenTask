# 文件改动记录

由 OpenCode 按 `_config.json` 的 `auto_track_changes` 和 `change_log_level` 维护。
`step` 档逐步追加，`feature` 档阶段汇总，`manual` 档仅在 RecordTask 或显式记录时整理。

| 时间 | 文件 | 改动类型 | 原因 |
|------|------|---------|------|
